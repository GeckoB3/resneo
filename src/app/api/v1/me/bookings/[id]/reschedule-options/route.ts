/**
 * The v1 alias required by C7a. A re-export rather than a parallel
 * implementation, so the two cannot drift.
 */
export { GET } from '@/app/api/account/bookings/[id]/reschedule-options/route';
