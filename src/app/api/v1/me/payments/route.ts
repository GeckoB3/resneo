/**
 * The v1 alias required by C7a for every route this plan creates. A re-export
 * rather than a parallel implementation, so the two cannot drift.
 */
export { GET } from '@/app/api/account/payments/route';
