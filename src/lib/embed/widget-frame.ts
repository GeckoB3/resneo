/**
 * Embedded booking iframe: default <iframe height="…"> in snippets and floor for
 * postMessage-reported height (iframe grows when content needs more space).
 */
export const EMBED_IFRAME_DEFAULT_HEIGHT_PX = 450;
export const EMBED_IFRAME_MIN_REPORTED_HEIGHT_PX = EMBED_IFRAME_DEFAULT_HEIGHT_PX;
